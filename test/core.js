'use strict';
const assert=require('assert');const Module=require('module');const old=Module._load;Module._load=function(r,p,m){if(r==='express'){const e=()=>{const a=function(){};a.use=()=>a;a.get=()=>a;return a};e.static=()=>()=>{};return e}if(r==='qrcode')return{toDataURL:async()=>''};if(r==='socket.io')return{Server:class{on(){}to(){return{emit(){}}}}};return old.call(this,r,p,m)};const core=require('../server');Module._load=old;
function upgrades(){return{fertilizer:0,shampoo:0,catapult:0,fort:0,repair:0,sheepTech:0,ramTech:0,goatTech:0}}function room(w=0){return{game:{startedAt:Date.now(),weather:{type:'sunny',sun:72,wind:w},players:[{upgrades:upgrades()},{upgrades:upgrades()}]}}}
const sheep={id:'s',kind:'sheep',bodyMass:64,fur:5,look:'zez',name:'S'};const ram={id:'r',kind:'ram',bodyMass:100,fur:5,look:'wielkooki',name:'R'};const goat={id:'g',kind:'goat',bodyMass:48,fur:3,look:'brodata',name:'G'};
assert.strictEqual(core.CONFIG.baseHp,5000);assert.strictEqual(core.CONFIG.unitLimit,20);assert.strictEqual(core.FLOCKS.length,10);assert.deepStrictEqual(Object.keys(core.UNIT_TYPES).sort(),['goat','ram','sheep']);assert(core.unitTotalMass(sheep,room().game.weather)>sheep.bodyMass);assert(core.unitAero({...sheep,fur:1.2},room().game.weather,room().game.players[0])>core.unitAero({...sheep,fur:12},room().game.weather,room().game.players[0]));assert.strictEqual(core.visualTier(0),1);assert.strictEqual(core.visualTier(3),2);assert.strictEqual(core.visualTier(5),3);
const calm=core.simulateShot(room(0),0,sheep,45,82,0,0);const windy=core.simulateShot(room(16),0,sheep,45,82,0,0);assert(calm.path.length>20);assert(Number.isFinite(calm.impact.x));assert(Math.abs(windy.impact.x-calm.impact.x)>15);assert.strictEqual(core.UNIT_TYPES.ram.cost,core.UNIT_TYPES.sheep.cost*2);assert.strictEqual(core.UNIT_TYPES.goat.cost,core.UNIT_TYPES.sheep.cost/2);assert.strictEqual(core.UNIT_TYPES.ram.damage,2);assert.strictEqual(core.UNIT_TYPES.goat.damage,.7);assert.strictEqual(core.UNIT_TYPES.goat.wool,0);assert(core.simulateShot(room(),0,ram,42,90,0,0).flight.mass>core.simulateShot(room(),0,goat,42,90,0,0).flight.mass);console.log('Core test OK');
